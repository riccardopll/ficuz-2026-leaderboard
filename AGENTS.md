# AGENTS.md

Leaderboard app (Astro + Cloudflare D1) for tracking player points and penalties.

## Commands

```bash
npm run db:migrate        # Apply migrations to remote D1
npm run db:migrate:local  # Apply migrations to local D1
npm run lint              # Format the repo and sort package.json
npm run typecheck         # Run after every code change
```
