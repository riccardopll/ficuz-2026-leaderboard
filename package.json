{
  "name": "ficuz-2026-leaderboard",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "lint": "npx prettier . --write",
    "dev": "npx astro build && npx wrangler dev",
    "deploy": "npx astro build && npx wrangler deploy"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^12.6.12",
    "@tailwindcss/vite": "^4.1.18",
    "astro": "^5.16.11",
    "tailwindcss": "^4.1.18"
  },
  "devDependencies": {
    "prettier": "3.8.0",
    "wrangler": "^4.59.2"
  }
}
